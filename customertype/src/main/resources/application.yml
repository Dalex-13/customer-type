
server:
  port: 8080 #${PORT:0}
spring:
  application:
    name: Customer-Type
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: CustomerType

eureka:
  client:
  service-url:
  defaultZone: http://localhost:8761/eureka

